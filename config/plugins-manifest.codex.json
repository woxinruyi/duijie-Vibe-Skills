{
  "version": 1,
  "profile": "codex",
  "core": [
    {
      "name": "superpowers",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command).",
      "required": true
    },
    {
      "name": "everything-claude-code",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command).",
      "required": true
    },
    {
      "name": "claude-code-settings",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command).",
      "required": true
    },
    {
      "name": "hookify",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command).",
      "required": true
    },
    {
      "name": "ralph-loop",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command).",
      "required": true
    }
  ],
  "optional": [
    {
      "name": "serena",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command)."
    },
    {
      "name": "context7",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command)."
    },
    {
      "name": "github",
      "install_mode": "manual-codex",
      "install_hint": "Provision via Codex-native MCP/plugin tooling (no Claude CLI command)."
    },
    {
      "name": "claude-flow",
      "install_mode": "scripted",
      "install": "npm install -g claude-flow"
    },
    {
      "name": "open-ralph-wiggum",
      "install_mode": "scripted",
      "install": "npm install -g @th0rgal/ralph-wiggum",
      "install_hint": "Used by `ralph-loop --engine open` as an optional auto-iteration backend."
    }
  ],
  "notes": "Codex compatibility rewrites are bundled in this repository; plugin metadata is advisory and no Claude CLI plugin command is executed."
}
