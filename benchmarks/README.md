# Benchmarks

This directory contains bounded downstream benchmark repositories used to test project delivery under `vibe`.

The goal is to validate work completed under governance against realistic project surfaces, not only against VibeSkills self-artifacts.

Each benchmark should include:

- a small repo-shaped fixture
- an `acceptance-manifest.json`
- one or more scenario mappings from `tests/scenarios/project_delivery/`
